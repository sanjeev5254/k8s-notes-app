apiVersion: v1
kind: Service
metadata:
  name: notes-app-service
spec:
  type: NodePort
  selector:
    app: notes-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30008
